# config/default.yml
AllCops:
  Include:
    - "**/*.yaml"
    - "**/*.yml"

YAML/Lint/InvalidSyntax:
  Description: Reports malformed YAML using the parser's best source location.
  Enabled: true
  VersionAdded: "0.1"
